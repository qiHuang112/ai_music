# 发现 · 音乐榜单

2026-09-26 需求：AI Music 首页新增“发现”，分 QQ 音乐和网易云音乐两个模块。QQ 模块按截图列出巅峰榜下的飙升榜、热歌榜、新歌榜、流行指数榜、听歌识曲榜、MV 榜；网易云模块按截图列出云音乐特色榜下的飙升榜、新歌榜、原创榜、热歌榜。点进任意榜单查看平台当前返回的完整排名、歌名、歌手和更新时间，允许刷新。榜单加载失败时保留错误和重试入口，不把旧数据说成新数据。

数据直接来自平台网页使用的接口：QQ 的 `musicToplist.ToplistInfoServer.GetDetail`，一次请求该榜最多 300 条；网易云的 `/api/playlist/detail?id=...`，读取响应里的全部 `tracks`。2026-09-26 实际探测：QQ 热歌榜返回 300 条，QQ MV 榜返回 20 条视频，网易云飙升/新歌/原创各 100 条、热歌 200 条。MV 榜的条目是视频而非音频，页面只展示排名，不支持加入音频歌单。

普通歌曲榜支持逐首勾选、全选和批量加入目标自建歌单，也可新建歌单。导入沿用截图导入的 `ScreenshotMatcher`：以榜单歌名搜索，只接收歌名和完整歌手集合一致的结果，保留榜单顺序；匹配并发数沿用“截图搜歌并发数”设置。只保存可信的在线候选，未找到的歌曲计入失败数，不写入歌单；再次选择同一歌曲时歌单层按候选 ID 去重，提示展示实际新增数。歌源错误或离开页面后停止后续匹配，错误时不写入歌单。榜单行的搜索按钮也可跳到首页原有搜歌结果，播放和下载继续走现有流程。批量加入本身不自动下载，后续进入歌单的 WiFi 自动下载仍遵守既有设置和 24 小时限制。

验证：Flutter 全量测试 301/301、`flutter analyze --no-pub` 0 问题、`git diff --check` 和 Android arm64 profile 构建通过。保留数据安装到 Mi 10 Pro，包内 versionCode 为 6106；本地 APK 与手机 `base.apk` SHA-256 均为 `d9422c9bd2aedabb3d8b1d4703050a8002063629c9a6f21e523e0b4de62f6f8e`，首次安装时间和搜狗输入法未变。真机首页显示两个平台模块；QQ 热歌榜显示“茶汤 / 郁可唯”“我不难过 / 孙燕姿”，网易云热歌榜显示“明知故犯 / Max李玄”“海屿你 / 马也_Crabbit”，单首勾选从 0 变为 1。未在用户手机上执行实际批量匹配或写入歌单，需用户验收。当前改动未走独立 review，未暂存、提交或推送。

2026-09-27 体验调整：将首页“发现”移到自建歌单列表之后；歌单详情右上角的“调整顺序”改为单个图标按钮，保留无障碍提示；下载管理的歌单条目可点击进入同一详情页，右侧下载按钮仍直接下载。完整 Flutter 测试 302/302、analyze 0、diff-check 和 arm64 profile 构建通过。保留数据安装 versionCode 6107，手机 `base.apk` 与本地 SHA-256 均为 `badd115d3f39b6dd6737a76cbc4f70ebdf26385c99e2eb5ed4c4604d950d52a8`，原始首次安装时间及搜狗输入法未变。真机显示“发现”位于自建歌单之后；从下载管理打开全部已缓存的“快乐”歌单进入详情，看到图标式调整顺序。没有触发该歌单下载，也没有修改歌单歌曲。改动仍未独立 review、暂存、提交或推送。

2026-09-27 独立 review：发现候选误选、取消后继续搜索、刷新与导入竞态、重复导入数量提示四个问题并已修复。全量 Flutter 测试 308/308、analyze 0、diff-check 通过；reviewer 独立运行相关测试 118/118 并确认无剩余阻断项。修复后的代码尚未重新构建或安装到手机；当前安装的 6107 包仍是 review 前版本。代码未暂存、提交或推送。
