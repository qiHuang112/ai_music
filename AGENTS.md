# AI Music 当前协作规则

生效日期：2026-07-16

AI Music 当前只使用 `mobile-ai-music` 四角色敏捷团队：

- `mobile-ai-music-产品`：把用户需求整理为不超过 5 条可验收标准。
- `mobile-ai-music-UX设计`：负责用户流程、界面规范和体验验收。
- `mobile-ai-music-开发`：负责公共 Dart、Android、HarmonyOS、iOS、测试和可安装包。
- `mobile-ai-music-负责人`：唯一任务入口，负责拆分、派工、review、集成、提交、推送和通知用户。

团队清单以 `docs/codex_collab/teams/mobile-ai-music/team.json` 为准。旧线程、旧 lane 和旧多角色说明只保留为历史来源，不得作为新任务的 owner 或审批链。

## 模型策略

- 产品、UX设计、开发、负责人统一继承用户当前全局默认模型与思考强度。
- 新派工、续接、恢复、替换 Agent 和协同账本不得写死或覆盖模型、思考强度；只有用户明确要求任务级例外时才能调整。
- 用户调整全局默认后，已运行任务继续执行，不得仅为模型策略变化重启、暂停或重建团队。

## 执行优先级

- P1：核心功能可用、可安装、可体验。先交 debug 包和真实设备主路径，再补非阻塞细节。
- P2：视觉细化、长尾状态、跨端补齐和自动化增强，不阻塞 P1 出包。
- P3：观感、命名和历史债，只记录进后续队列。

## 当前交付主线

- 唯一产品交付主线是 `/Users/huangqi/AIHome/ai_music_android_native` 的 Android Native 工程，当前集成分支 `codex/native-unified-epic-20260726`，冻结起点 `96093aa771e3a89ff11d523ed99fcacfeaa9b8ee`。所有新功能从该分支最新确认 HEAD 继续。
- `/Users/huangqi/AIHome/ai_music` 是协作管理根；其中 Flutter 实现只保留历史证据、紧急回退和必须追平的播放/歌词/进度 UX 合同，不得修改为本轮交付代码。旧歌源专项和小爱专项只作为原生 Epic 的历史输入或替代项，不再继续创建互相等待的窄 request。
- HarmonyOS 与 iOS 当前只允许研究、协议验证和风险沉淀，不得阻塞 Android Native P1。
- 当前唯一活跃工作流是 `ai-music-rapid-delivery-v2`，状态只允许 `active`、`integrating`、`candidate_ready`、`needs_user_acceptance`、`complete`。`superpowers-v1` 仅可出现在历史或关闭记录中，不得驱动活跃 Epic。
- Android Native 以单一 Epic 持续集成。负责人只负责派工、集成、review、stage、commit 和 push，不承担日常功能开发；开发是统一集成者，在自己的持久任务内管理公开歌源、多 Provider 聚合、Flutter UX 等价迁移、自动化与证据四条互斥写集执行线。
- 四条执行线必须使用独立完整 clone，禁止 worktree。任一独立切片完成即进入统一工程集成；任何执行线 15 分钟没有新增事实，开发必须收窄范围或在同一任务内替换执行者。
- 公开歌源研究必须低压、合法且 fail closed：不得绕过验证码、防护、登录、付费或 DRM；不得把网盘、试听、HTML、防护页或错误歌手冒充完整音频。单 Provider 最多 1 个并发，全局最多 2 个并发；单轮研究最多 20 次请求，同源请求间隔至少 1.5 秒。连续 3 次传输失败冷却 15 分钟，遇到 403、429 或防护页立即暂停该来源，其他 Provider 继续。
- 逻辑验收前禁止安装中间包。至少两个公开 Provider 通过严格完整音频准入，并且 fresh tests/lint/build、完整搜索、聚合分页、Media3 播放、边播 seek、下载转正、歌词封面、队列、失败隔离和 evidence manifest 全通过后，只安装一次小米 10 Pro 功能候选并进入用户逻辑验收；最终真实 UI 全接线后再安装一次进入真实 UI 验收。

## 自动执行

- 用户给出的明确需求视为需求门禁已通过，不重复索要确认。
- 日常文件编辑、测试、构建、ADB/HDC、loopback/socket、debug 安装和证据采集不得向用户请求授权。
- 遇到平台安全限制、签名、审核或真实设备锁屏等无法自行解决的问题，10 到 15 分钟内直接回报核心问题、已尝试路径和替代方案。
- 开发不因 review、设备证据或 P2 等待而停工；独立事项并行，存在依赖时先完成不依赖部分。
- 不使用 Git worktree。并行开发使用独立完整 clone/Project Path，并以最新目标分支作为基线。
- 开发者完成约 80% 核心路径后直接提供可体验包；用户反馈进入 discovery inbox，负责人据此继续派工。

## 持续敏捷与停点

- 团队默认持续推进，不因测试通过、APK 构建、专项完成、达到 80%、review accepted、stage、commit 或 push 停止工作或把控制权退回用户。
- 只有三种情况允许停止并回用户：需求真正完成；必须由用户验收逻辑功能或完整 UI；最终真实 UI 已落地完成、需要用户验收。除此之外，负责人必须继续派工、整合、回改、构建、装机、review 或合入。
- 产品把抽象需求收敛到足够开发后，开发逻辑与 UX 设计并行；UX 初稿一旦可实施，开发立即落大体效果，不等待完整设计包或阶段性批准。
- 用户可以直接与开发调整业务逻辑或 UI，也可以直接要求 UX 基于当前实现重新设计；设计稿允许包含尚未实现的功能，但在进入实现前由负责人检查其与冻结业务范围、现有能力和安全边界的冲突。
- 所有直接沟通产生的用户可见变化、替换和未决点必须写入 `docs/codex_collab/teams/mobile-ai-music/discovery-inbox.json`。产品和 UX 在下一轮分别吸收、标记 superseded 或说明不适用；负责人检查最新意图冲突、同步开发并持续推进。
- “设计稿 UI 已验收”只表示视觉/交互规范被冻结；“真实落地 UI 已完成验收”必须基于最新可安装包、真实设备、关键状态和用户体验结论。两者不得互相代签或混写为完成。

## 合入权责

- 只有负责人可以 stage、commit、merge、push 和发布。
- 产品、UX、开发只提交证据和 handoff，不各自维护合入链。
- 每次合入必须整合目标分支已有功能，禁止旧基线回退。
- 提交前按仓库实际规则检查目标分支、PR/MR 或普通分支路由，不增加项目外部编号门禁。

## 质量底线

- Bug 先复现和定位根因；功能与修复默认 RED-GREEN 测试。
- “完成”必须有 HEAD、测试、包 SHA、设备、操作步骤和关键截图/日志。
- AI Music 搜索结果只把完整可播放音频视为成功；PREVIEW、网盘、HTML、防护页和低置信错误匹配必须 fail closed。
- 核心音乐路径必须覆盖搜索、完整播放、下载、边下边播、seek、歌词、封面、缓存转正及失败不污染缓存。
